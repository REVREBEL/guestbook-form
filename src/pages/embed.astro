---
/**
 * Guestbook Embed Page - Minimal version for iframe embedding
 * 
 * This page renders JUST the guestbook button with NO container constraints.
 * Designed to be embedded in an iframe on any page.
 * 
 * Usage:
 * <iframe src="[your-base-url]/embed" style="border:none; width:auto; height:auto; overflow:visible;"></iframe>
 */

import '../../generated/fonts.css';
import '../site-components/global.css';
import '../styles/global.css';
import { GuestbookButton } from '../components/GuestbookButton';
---

<!DOCTYPE html>
<html lang="en" style="background: transparent;">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Guestbook Button</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    html, body {
      background: transparent !important;
      background-color: transparent !important;
      overflow: visible !important;
      width: auto !important;
      height: auto !important;
      min-height: 0 !important;
      min-width: 0 !important;
    }
    
    /* Override any theme backgrounds */
    body, body.dark, body.__DARK_MODE_CLASS__ {
      background: transparent !important;
      background-color: transparent !important;
    }
    
    /* NO grid, NO flex, NO constraints - just inline content */
    body {
      display: inline-block;
      width: auto;
      height: auto;
    }
  </style>
</head>
<body>
  <GuestbookButton client:only="react" />
</body>
</html>
